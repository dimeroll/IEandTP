@model Lab1_ICtaTP.TrainType

@{
    ViewData["Title"] = "Створити тип потяга";
}

<h1>Створити</h1>

<h4>тип потяга</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="TrainTypeName" class="control-label"></label>
                <input asp-for="TrainTypeName" class="form-control" />
                <span asp-validation-for="TrainTypeName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
